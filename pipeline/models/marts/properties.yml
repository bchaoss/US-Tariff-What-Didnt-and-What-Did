version: 2

models:
  - name: fact_us_treasury_statement_monthly
    description: 'desc'
    columns:
      - name: statement_type
        tests:
          # - unique
          - not_null
      
exposures:
  - name: dashboard1
    # label: 
    type: dashboard
    maturity: high
    # url: 
    description: 'desc'
    # depends_on: 
    owner:
      name: Pubilc