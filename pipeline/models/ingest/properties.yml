version: 2

models:
  - name: enduse_code_export
    description: 'desc'
    config:
      tags: ['static', 'trade']

  - name: enduse_code_import
    description: 'desc'
    config:
      tags: ['static', 'trade']